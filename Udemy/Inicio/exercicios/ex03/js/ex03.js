let n1 = window.prompt('Digite um número:');
let n2 = window.prompt('Digite outro número:');

const res = Number(n1) + Number(n2);

window.alert(`A soma dos números é: ${res}`)
window.alert(`A soma dos números é: ${Number(n1) + Number(n2)}`) // Não recomendado, mas é possível de se fazer
