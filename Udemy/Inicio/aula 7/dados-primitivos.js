const nome = 'Luiz'; // string
const nome1 = "Luiz"; // string
const nome2 = `Luiz`; // string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // Quando é declarada desta forma, o tipo de dado é "undefined" = não aponta para local nenhum na memória.
let sobrenomeAluno = null; // Nulo - não aponta para local nenhum na memória.
const aprovado = true; // Boolean = true ou false (valor lógico).

console.log(typeof nome, nome);